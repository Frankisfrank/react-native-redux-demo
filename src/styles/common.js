import {
    StyleSheet
} from 'react-native';
export default StyleSheet.create({
    container: {
        marginTop: 20,
        padding: 20,
    },
    rowFlexBox: {
        flexDirection: 'row',
        marginTop: 10,
    },
    title: {
        fontSize: 26,
        textAlign: 'center',
    },
    button: {
        backgroundColor: '#4389F9',
        justifyContent: 'center',
        alignItems: 'center',
    },
});
export const constant = { underlayColor: "#4378F6" }